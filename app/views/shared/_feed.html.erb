<% if @feed_items.any? %>
	<ol class="microposts">
		<!-- 传入一个名为 :collection 的参数， 其值为@feed_items, 这样render方法就会在指定的局部视图中使用这个集合中的数据渲染各项目.(之前，在渲染局部视图时，我们都省略了 :partial参数，例如 render 'shared/micropost', 不过如果指定了 :collection参数，这种省略的用法是错误的) -->
		<%= render partial: 'shared/feed_item', collection: @feed_items %>
	</ol>
	<%= will_paginate @feed_items %>
<% end -%>