<% provide(:title, 'All users') %>

<h1>All users</h1>
<%= will_paginate %>
<ul class="users">
<% #Rails 会把@users当做一系列的User对象，遍历这些对象，然后用_user.html.erb渲染每个对象 %>
<%= render @users %>

</ul>

<%= will_paginate %> 